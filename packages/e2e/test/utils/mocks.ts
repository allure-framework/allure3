/*import { z } from "zod";
import { generateMock: zMock } from "@anatine/zod-mock";

export const generateItem = (schema: z.ZodSchema, generatorFunction: (record: ReturnType<typeof zMock>) => any) => {
  return zMock(schema);
};

export const generateItems = (count: number, schema: z.ZodSchema) => {
  return Array.from({ length: count }, () => generateItem(index, schema));
};*/
